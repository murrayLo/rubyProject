<%= form_with(model: lecturer) do |form| %>
  <% if lecturer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(lecturer.errors.count, "error") %> prohibited this lecturer from being saved:</h2>

      <ul>
        <% lecturer.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <!--<div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>-->

  <div class="field">
    <%= form.label :title %>
    <%= form.select :title, ['Mr', 'Mrs', 'Ms', 'Miss', 'Dr', 'Prof'] %>
  </div>

  <div class="field">
    <%= form.label :fname %>
    <%= form.text_field :fname %>
  </div>

  <div class="field">
    <%= form.label :sname %>
    <%= form.text_field :sname %>
  </div>

  <div class="field">
    <%= form.label :subject %>
    <%= form.text_field :subject %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
